# Google Gemini API
GOOGLE_API_KEY=your_gemini_api_key_here

# Embeddings
EMBEDDING_MODEL=models/embedding-001

# LLM Model
LLM_MODEL=gemini-2.5-flash

# Evidence storage
# Set to 'false' to skip storing evidence in Chroma and avoid embedding calls
STORE_EVIDENCE=true
